# Завдання 3
# Напишіть функцію, яка обчислює кількість символів у заданому рядку.
# вхід: "hello"
# вихід: {"h":1, "e":1, "l":2, "o":1}
def kilkistcharsiv(text):
    result = {}
    for i in text:
        if i in result:
            result[i] += 1 
        else:
            result[i] = 1
    return result
print(kilkistcharsiv("hello"))